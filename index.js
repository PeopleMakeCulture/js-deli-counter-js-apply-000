function takeANumber(line, person){
  //adds a person to line
  line.push(person);
  console.log(person);
  //returns message
  console.log ("Welcome, ${person}.") //You are number ${line.length + 1} in line."
}

function nowServing(line){
  if (line.length = 0){
    return 'There is nobody waiting to be served!'
  }
}

function currentLine(){}
